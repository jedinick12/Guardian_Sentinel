using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyController : MonoBehaviour
{
    // Start is called before the first frame update
    // TODO: 
    // setup patrolling point/area(it round shape)
    // chasing the player in certain distance
    // enemey HP, and decrease player HP
    // attack rate, and attack speed or fireball


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
